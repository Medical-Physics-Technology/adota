"""The DoTA3D_v3 model and the configuration it is built from.

Modules:
- ``config``: YAML loading, run-directory setup, logging, shared defaults.
- ``layers``: the ``nn.Module`` building blocks.
- ``models``: ``DoTA3D_v3`` itself, assembled from those blocks.
- ``utils``: checkpoint loading and parameter counting.
"""
